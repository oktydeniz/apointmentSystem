<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.appointmentsystem">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppointmentSystem">
        <activity
            android:name=".OpeningActivity"
            android:exported="true"
            android:label="@string/title_activity_opening"
            android:theme="@style/Theme.AppointmentSystem.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".views.app.AppActivity"
            android:exported="true" />
        <activity
            android:name=".views.user.MainActivity"
            android:exported="true" />
    </application>

</manifest>